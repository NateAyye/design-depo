import { useToast } from "../components/ui/use-toast"

function Home() {
  const { toast } = useToast()

  return (
    <div className="container">
      Home
     
    </div>
  )
}

export default Home