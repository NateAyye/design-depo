{
  "name": "group-project-03",
  "version": "1.0.1",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js --ignore client",
    "develop": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "install": "cd server && npm i --force && cd ../client && npm i --force",
    "seed": "cd server && npm run seed",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "build": "cd client && npm run build",
    "tailwind:dev": "cd client && npm run tailwind:dev",
    "tailwind:prod": "cd client && npm run tailwind:prod"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.1",
    "prettier": "^3.0.3"
  }
}
