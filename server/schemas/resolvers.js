const { Tech, Matchup } = require('../models');

const resolvers = {
  Query: {
    : async () => {
     
    },
    : async (parent, { _id }) => {
      
    },
  },
  Mutation: {
    
  },
};

module.exports = resolvers;
